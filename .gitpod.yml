---
tasks:
  - name: Docker
    init: >
      ssh-keyscan github.com >> /home/gitpod/.ssh/known_hosts &&
      git submodule update --init
    command: >
      echo "command"
github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
    addCheck: true
    addComment: true
    addBadge: true
