---
tasks:
  - name: Docker
    init: >
      git submodule update --init
    command: >
      git submodule update --init
github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
    addCheck: true
    addComment: true
    addBadge: true
