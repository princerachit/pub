---
tasks:
  - name: Docker
    init: >
      git submodule update --init --quiet
    command: >
      echo "command"
github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
    addCheck: true
    addComment: true
    addBadge: true
